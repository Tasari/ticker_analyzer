# Analysis Package

This package owns the analysis workflow: fetching data through the provider, selecting a scoring profile, building metrics, scoring tabs, and assembling the final result.

Financial formulas belong in `ticker_analyzer.metrics`, not in this package.
